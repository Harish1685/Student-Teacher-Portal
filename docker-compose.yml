
services:
  database:
    container_name: database
    image: mysql:8.0
    networks:
      - my-network
    volumes:
      - mysql-data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE:  my-db
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-proot"]
      interval: 10s
      timeout: 5s
      retries: 5


  backend:
    container_name: backend
    image: zorochan/backend:latest
    ports:
     - "3500:3500"
    networks:
      - my-network
    environment:
      DB_HOST: database
      DB_USER: root
      DB_PASSWORD: root
      DB_NAME: my-db
      DB_PORT: 3306
    depends_on:
      database:
        condition: service_healthy
  frontend:
    container_name: frontend
    image: zorochan/frontend:latest
    networks:
      - my-network
    ports:
      - "80:80"
    
    depends_on:
      backend:  
        condition: service_started

    environment:
      NODE_ENV: production

networks:
  my-network:


volumes:
  mysql-data: